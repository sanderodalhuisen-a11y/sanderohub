entrypoint = "npm run dev -- --host 0.0.0.0 --port 3000"
modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "npm install && npm run dev -- --host 0.0.0.0 --port 3000"]

autoDeploy = false

[[ports]]
localPort = 3000
externalPort = 80
