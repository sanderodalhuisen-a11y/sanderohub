run = "npm install && npm run start"
entrypoint = "index.html"
modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "npm install && npm run start"]

autoDeploy = false

[[ports]]
localPort = 3000
externalPort = 80
